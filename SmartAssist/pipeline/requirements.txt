# SmartAssist Pipeline Requirements
# Install these AFTER installing root requirements.txt

# IMPORTANT: GStreamer and DeepStream CANNOT be pip installed
# They must be installed via system package manager (apt)
# 
# Required system packages (install with apt):
#   sudo apt install python3-gi python3-gi-cairo gir1.2-gstreamer-1.0
#   sudo apt install gstreamer1.0-tools gstreamer1.0-plugins-good
#   sudo apt install gstreamer1.0-plugins-bad gstreamer1.0-plugins-ugly
#   sudo apt install deepstream-6.x (from NVIDIA SDK Manager)

# No additional pip packages needed for pipeline
# All Python dependencies are in root requirements.txt