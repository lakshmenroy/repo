# Following properties are mandatory when engine files are not specified:
#   int8-calib-file(Only in INT8), model-file-format
#   Caffemodel mandatory properties: model-file, proto-file, output-blob-names
#   UFF: uff-file, input-dims, uff-input-blob-name, output-blob-names
#   ONNX: onnx-file
#
# Mandatory properties for detectors:
#   num-detected-classes
#
# Optional properties for detectors:
#   cluster-mode(Default=Group Rectangles), interval(Primary mode only, Default=0)
#   custom-lib-path,
#   parse-bbox-func-name
#
# Mandatory properties for classifiers:
#   classifier-threshold, is-classifier
#
# Optional properties for classifiers:
#   classifier-async-mode(Secondary mode only, Default=false)
#
# Optional properties in secondary mode:
#   operate-on-gie-id(Default=0), operate-on-class-ids(Defaults to all classes),
#   input-object-min-width, input-object-min-height, input-object-max-width,
#   input-object-max-height
#
# Following properties are always recommended:
#   batch-size(Default=1)
#
# Other optional properties:
#   net-scale-factor(Default=1), network-mode(Default=0 i.e FP32),
#   model-color-format(Default=0 i.e. RGB) model-engine-file, labelfile-path,
#   mean-file, gie-unique-id(Default=0), offsets, process-mode (Default=1 i.e. primary),
#   custom-lib-path, network-mode(Default=0 i.e FP32)
#
# The values in the config file are overridden by values set through GObject
# properties.

## Configures the general behavior of the Plugin
[property]
## Device ID of GPU to use for pre-processing/inference
gpu-id=0
## Mode (primary=1 or secondary=2) in which the element is to operate on (ignored if input-tensor-meta enabled)
process-mode=1
## Array of mean values of color components to be subtracted from each pixel
offsets=123.675;116.28;103.53
## Pixel normalization factor
net-scale-factor=0.017352074
## Color format required by the model (0=RGB, 1=BGR, 2=GRAY)
model-color-format=0 
## Absolute pathname of the pre-generated serialized engine file for the mode
model-engine-file=/mnt/ssd/SmartAssist/models/csi/weights/v2.0.0/road_segmentation.engine
## Defines the dimensional parameters for inference (channels;height:width)  
infer-dims=3;416;608  
## Number of frames or objects to be inferred together in a batch
batch-size=2
## Data format to be used by inference (0=FP32, 1=INT8, 2=FP16 mode)
network-mode=2
## Number of classes detected by the network (background, road)
num-detected-classes=2 
## Number of consecutive batches to be skipped for inference
interval=0
## Unique ID to be assigned to the GIE to enable the application and other elements to identify detected bounding boxes and labels
gie-unique-id=2
## Type of network (0=Detector, 1=Classifier, 2=Segmentation, 3=Instance Segmentation)
network-type=2
## Model network output layers' names (ONNX output layer name)
output-blob-names=output
## Confidence threshold for the segmentation model to output a valid class for a pixel. If confidence is less than this threshold, class output for that pixel is −1
segmentation-threshold=0.0

## Name of the custom bounding box parsing function. If not specified, Gst-nvinfer uses the internal function for the resnet model provided by the SDK
#parse-bbox-func-name=NvDsInferParseCustomSSD
## Absolute pathname of a library containing custom method implementations for custom models
#custom-lib-path=nvdsinfer_custom_impl_ssd/libnvdsinfer_custom_impl_ssd.so
## The filter to use for scaling frames / object crops to network resolution (ignored if input-tensor-meta enabled)
#scaling-filter=0
## Compute hardware to use for scaling frames / object crops to network resolution (ignored if input-tensor-meta enabled) (0=Platform default – GPU (dGPU), VIC (Jetson), 1=GPU, 2=VIC (Jetson only))
#scaling-compute-hw=0

## Configures detection parameters for all classes
[class-attrs-all]
## Offset of the RoI from the top of the frame. Only objects within the RoI are output
roi-top-offset=0
## Offset of the RoI from the bottom of the frame. Only objects within the RoI are output
roi-bottom-offset=0
## Minimum width in pixels of detected objects to be output by the GIE
detected-min-w=0
## Minimum height in pixels of detected objects to be output by the GIE
detected-min-h=0
## Maximum width in pixels of detected objects to be output by the GIE
detected-max-w=0
## Maximum height in pixels of detected objects to be output by the GIE
detected-max-h=0

## Per class configuration
#[class-attrs-2]
## Detection threshold
#threshold=0.6
## Offset of the RoI from the top of the frame. Only objects within the RoI are output
#roi-top-offset=20
## Offset of the RoI from the bottom of the frame. Only objects within the RoI are output
#roi-bottom-offset=10
## Minimum width in pixels of detected objects to be output by the GIE
#detected-min-w=40
## Minimum height in pixels of detected objects to be output by the GIE
#detected-min-h=40
## Maximum width in pixels of detected objects to be output by the GIE
#detected-max-w=400
## Maximum height in pixels of detected objects to be output by the GIE
#detected-max-h=800